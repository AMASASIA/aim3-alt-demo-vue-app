const axios = require('axios');

async function checkAI(prompt, key) {
    // Utility for server-side AI checks if needed
    // Similar implementation to frontend, but using axios
}

module.exports = { checkAI };
